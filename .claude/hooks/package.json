{
  "name": "ogmios-hooks",
  "version": "1.0.0",
  "description": "TypeScript hooks for Ogmios PAI system - automatic context loading and skill activation",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "bun test",
    "type-check": "tsc --noEmit",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write \"**/*.{ts,json,md}\"",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@types/node": "^20.10.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.55.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "keywords": [
    "claude",
    "claude-code",
    "pai",
    "ogmios",
    "hooks",
    "automation",
    "context-loading",
    "skill-activation"
  ],
  "author": "<YOUR_NAME>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "PRIVATE - Do not commit PAI to public repos!"
  },
  "comments": {
    "installation": "Run 'bun install' in ~/.claude/.claude/hooks/ directory",
    "usage": "Hooks are automatically executed by Claude Code based on settings.json configuration",
    "bun": "This project uses Bun as the package manager and runtime. Install from https://bun.sh",
    "typescript": "All hooks written in TypeScript for type safety",
    "testing": "Add tests in __tests__/ directory and run with 'bun test'"
  }
}
